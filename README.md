## Init

```bash
# tailwindcss
yarn add -D tailwindcss postcss autoprefixer
yarn tailwindcss init tailwind.config.cjs -p

# daisyUI
yarn add -D daisyui
yarn add -D @tailwindcss/typography

# supabase
yarn add @supabase/supabase-js
yarn add @supabase/auth-helpers-sveltekit
```